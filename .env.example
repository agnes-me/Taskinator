# SQLite by default (fichier local). Pour la prod, pointer vers une base Postgres :
# DATABASE_URL="postgresql://user:password@host:5432/taskinator"
DATABASE_URL="file:./dev.db"

# Générer avec: openssl rand -base64 32
NEXTAUTH_SECRET="change-me-in-production"
NEXTAUTH_URL="http://localhost:3000"
